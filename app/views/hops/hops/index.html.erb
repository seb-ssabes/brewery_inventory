<header class="hops-header p-5 flex items-center justify-between z-50 fixed top-0 left-0 w-full">
  <h1 class="text-gray-800 flex-grow text-center text-3xl mx-auto">Substitutes</h1>
</header>

<form class="search-form items w-full max-w-md mx-auto p-4 bg-white rounded-lg shadow-md"
      data-controller="search">
  <input type="text" id="hopSearch" placeholder="Hop name"
         data-action="input->search#searchInput"
         data-action="keyup->search#searchInput"
         data-search-target="input"
         autocomplete="off"
         class="w-full px-4 mt-1 py-2 border border-gray-300 rounded-md focus:ring-3
          focus:ring-gray-500 focus:border-gray-600 transition duration-200" />
  <ul class="suggestions hidden bg-white mt-2 rounded-md shadow-sm text-lg
             overflow-hidden transition-opacity ease-in-out"
      data-action="click->search#selectSuggestion"
      data-search-target="suggestions"
      id="hopSuggestions">
  </ul>
</form>

<div class="relative overflow-x-auto p-2 mt-7 " id="table">
</div>

<footer class="bg-white p-2 flex items-center justify-center fixed bottom-0 w-1/2 left-1/2 transform -translate-x-1/2 shadow-top z-10 rounded-md">
  <div class="self-center">
    <%= link_to category_path(1), class: '' do %>
      <%= image_tag cl_image_path("brewery_inventory/back"), class: 'w-14' %>
    <% end %>
  </div>
</footer>
