<header class="bg-gray-100 p-6 flex items-center justify-center">
  <h1 class="text-3xl font-bold">Inventory</h1>
</header>

<div class="flex-1 p-6">
  <ul clas="flex flex-1 flex-col h-full">
    <% @categories.each do |category| %>
      <%= link_to category_path(category), class: 'block' do %>
        <li class="flex flex-1 items-center justify-between shadow-md p-6 rounded-lg">
          <div class="icon">
            <% if category.name == 'Hops' %>
              <i class="fa-solid fa-leaf"></i>
            <% elsif category.name == 'Malts' %>
              <i class="fa-solid fa-wheat-awn"></i>
            <% elsif category.name == 'Yeasts' %>
              <i class="fa-solid fa-flask"></i>
            <% end %>
          </div>
          <div class="category-content">
            <h3><%= category.name %></h3>
            <p><%= category.items.count %></p>
          </div>
        </li>
      <% end %>
    <% end %>
  </ul>
</div>
