<p><%= link_to item.name, category_item_path(@category, item) %></p>
<p><%= number_with_precision(item.alpha, precision: (item.alpha % 1 == 0 ? 0 : 1))%>%</p>
<p><%= item.item_type %></p>
<p><%= item.origin %></p>
